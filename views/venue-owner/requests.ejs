<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>سیستم مدیریت درخواست‌ها | صاحب مکان</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        * {
            font-family: 'Vazirmatn', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #c1ddec 0%, #e4b2e2 100%);
            min-height: 100vh;
            color: #39121d;
        }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        
        /* Gradient Text */
        .gradient-text {
            background: linear-gradient(135deg, #ec4899 0%, #a855f7 50%, #6366f1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        /* Status Badges */
        .status-badge {
            padding: 0.5rem 1.2rem;
            border-radius: 16px;
            font-size: 0.85rem;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .status-pending {
            background: linear-gradient(135deg, rgba(245, 158, 11, 0.2), rgba(217, 119, 6, 0.1));
            color: #d97706;
            border: 1px solid rgba(245, 158, 11, 0.3);
        }
        
        .status-accepted {
            background: linear-gradient(135deg, rgba(34, 197, 94, 0.2), rgba(22, 163, 74, 0.1));
            color: #16a34a;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }
        
        .status-rejected {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.2), rgba(220, 38, 38, 0.1));
            color: #dc2626;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }
        
        /* Buttons */
        .btn-primary {
            background: linear-gradient(135deg, #1565c0 0%, #42a5f5 100%);
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
            box-shadow: 0 8px 20px rgba(21, 101, 192, 0.3);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(21, 101, 192, 0.4);
        }
        
        .btn-secondary {
            background: rgba(255, 255, 255, 0.9);
            color: #1565c0;
            border: 2px solid rgba(21, 101, 192, 0.3);
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
        }
        
        .btn-secondary:hover {
            transform: translateY(-2px);
            border-color: rgba(21, 101, 192, 0.5);
            background: white;
        }
        
        /* Action Buttons */
        .action-btn {
            padding: 0.5rem 1rem;
            border-radius: 10px;
            transition: all 0.3s ease;
            font-size: 0.9rem;
            font-weight: 500;
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .btn-approve {
            background: linear-gradient(135deg, rgba(34, 197, 94, 0.2), rgba(22, 163, 74, 0.1));
            color: #16a34a;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }
        
        .btn-reject {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.2), rgba(220, 38, 38, 0.1));
            color: #dc2626;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }
        
        .btn-view {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(29, 78, 216, 0.1));
            color: #1d4ed8;
            border: 1px solid rgba(59, 130, 246, 0.3);
        }
        
        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        }
        
        /* Modal Styles */
        .modal {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(10px);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            padding: 1rem;
        }
        
        .modal.active {
            display: flex;
        }
        
        /* venue Selector */
        .venue-tab {
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.9);
            border: 2px solid rgba(21, 101, 192, 0.3);
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .venue-tab.active {
            background: linear-gradient(135deg, #1565c0 0%, #42a5f5 100%);
            color: white;
            border-color: transparent;
            box-shadow: 0 8px 20px rgba(21, 101, 192, 0.3);
        }
        
        /* Form Display Styles */
        .form-section-display {
            border-bottom: 2px solid rgba(14, 165, 233, 0.1);
            padding-bottom: 1.5rem;
            margin-bottom: 1.5rem;
        }
        
        .form-section-title {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1rem;
            color: #0369a1;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid rgba(14, 165, 233, 0.1);
        }
        
        .form-section-icon {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: linear-gradient(135deg, rgba(14, 165, 233, 0.2), rgba(56, 189, 248, 0.1));
            display: flex;
            align-items: center;
            justify-content: center;
            color: #0369a1;
            font-size: 1.2rem;
        }
        
        .form-field {
            background: rgba(241, 245, 249, 0.5);
            border: 2px solid rgba(14, 165, 233, 0.2);
            border-radius: 12px;
            padding: 1rem 1.5rem;
            margin-bottom: 1rem;
            transition: all 0.3s ease;
        }
        
        .form-field:hover {
            border-color: rgba(14, 165, 233, 0.4);
            background: rgba(241, 245, 249, 0.7);
        }
        
        .field-label {
            display: block;
            color: #0c4a6e;
            font-weight: 600;
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }
        
        .field-value {
            color: #1e40af;
            font-size: 1rem;
            line-height: 1.6;
        }
        
        .category-badge {
            background: rgba(14, 165, 233, 0.1);
            color: #0369a1;
            border: 1px solid rgba(14, 165, 233, 0.3);
            padding: 0.6rem 1.2rem;
            border-radius: 10px;
            font-size: 0.9rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        /* Price Badge */
        .price-badge {
            background: linear-gradient(135deg, #f59e0b, #d97706);
            color: white;
            padding: 0.4rem 1rem;
            border-radius: 12px;
            font-weight: 700;
            box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);
        }
        
        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: rgba(186, 230, 253, 0.3);
            border-radius: 10px;
        }
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, #0369a1, #0ea5e9);
            border-radius: 10px;
        }
        
        /* Table Styles */
        .request-row {
            border-bottom: 1px solid #e5e7eb;
            transition: all 0.3s ease;
        }
        
        .request-row:hover {
            background-color: rgba(59, 130, 246, 0.05);
        }
        
        .main-content {
            padding-left: 80px;
            padding-right: 80px;
            padding-bottom: 100px;
        }
        
        /* Utility Classes */
        .hidden {
            display: none !important;
        }
    </style>
</head>
<body class="min-h-screen p-4 md:p-10">
    
    <div class="main-content">
        <!-- Header -->
        <header class="mb-8">
            <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 mb-6">
                <div>
                    <h1 class="text-3xl pt-10 md:text-4xl font-bold gradient-text mb-2">سیستم مدیریت درخواست‌ها</h1>
                    <p class="text-blue-700">صاحب مکان: <span class="font-bold" id="ownerName"><%= user.name %></span></p>
                    <p class="text-blue-600 text-sm mt-1">مدیریت درخواست‌های رزرو مکان‌های شما</p>
                </div>
                
                <div class="flex items-center gap-4">
                    <button class="btn-secondary" onclick="showAddvenueModal()">
                        <i class="fas fa-plus ml-2"></i> افزودن مکان جدید 
                    </button>
                </div>
            </div>
           
            
            <!-- Stats Cards -->
            <% if (requests && requests.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
                    <div class="glass-card p-6">
                        <div class="flex items-center gap-4">
                            <div class="w-12 h-12 rounded-xl bg-gradient-to-r from-blue-100 to-cyan-100 flex items-center justify-center">
                                <i class="fas fa-inbox text-2xl text-blue-600"></i>
                            </div>
                            <div>
                                <p class="text-blue-500 text-sm mb-1">کل درخواست‌ها</p>
                                <h4 class="text-2xl font-bold text-blue-900"><%= requests.length %></h4>
                            </div>
                        </div>
                    </div>
                    
                    <div class="glass-card p-6">
                        <div class="flex items-center gap-4">
                            <div class="w-12 h-12 rounded-xl bg-gradient-to-r from-amber-100 to-yellow-100 flex items-center justify-center">
                                <i class="fas fa-clock text-2xl text-amber-600"></i>
                            </div>
                            <div>
                                <p class="text-blue-500 text-sm mb-1">در انتظار تایید</p>
                                <h4 class="text-2xl font-bold text-blue-900">
                                    <%= requests.filter(r => r.status === 'pending').length %>
                                </h4>
                            </div>
                        </div>
                    </div>
                    
                    <div class="glass-card p-6">
                        <div class="flex items-center gap-4">
                            <div class="w-12 h-12 rounded-xl bg-gradient-to-r from-green-100 to-emerald-100 flex items-center justify-center">
                                <i class="fas fa-check-circle text-2xl text-green-600"></i>
                            </div>
                            <div>
                                <p class="text-blue-500 text-sm mb-1">تایید شده</p>
                                <h4 class="text-2xl font-bold text-blue-900">
                                    <%= requests.filter(r => r.status === 'accepted').length %>
                                </h4>
                            </div>
                        </div>
                    </div>
                    
                    <div class="glass-card p-6">
                        <div class="flex items-center gap-4">
                            <div class="w-12 h-12 rounded-xl bg-gradient-to-r from-purple-100 to-pink-100 flex items-center justify-center">
                                <i class="fas fa-wallet text-2xl text-purple-600"></i>
                            </div>
                            <div>
                                <p class="text-blue-500 text-sm mb-1">درآمد کل</p>
                                <%
                                    let totalRevenue = 0;
                                    requests.forEach(r => {
                                        if (r.price && typeof r.price === 'number') {
                                            totalRevenue += r.price;
                                        }
                                    });
                                %>
                                <h4 class="text-2xl font-bold text-blue-900"><%= totalRevenue.toLocaleString('fa-IR') %> تومان</h4>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </header>

        <!-- Main Content -->
        <main class="relative">
            <!-- Search and Actions -->
            <div class="flex flex-col md:flex-row gap-4 mb-6">
                <div class="flex-1">
                    <div class="relative">
                        <input type="text" 
                               placeholder="جستجوی درخواست (سازمان، عنوان رویداد، تاریخ...)" 
                               class="w-full p-3 pr-12 rounded-xl border border-blue-200 focus:border-blue-400 focus:ring-2 focus:ring-blue-100 outline-none"
                               id="searchInput" oninput="searchRequests()">
                        <i class="fas fa-search absolute right-4 top-1/2 transform -translate-y-1/2 text-blue-400"></i>
                    </div>
                </div>
                
                <div class="flex gap-3">
                    
                    <button class="btn-secondary" onclick="window.location.href='/venue-owner/dashboard'">
                        <i class="fas fa-sync-alt ml-2"></i> بازگشت به داشبورد
                    </button>
                </div>
            </div>
            
            <!-- Requests List -->
            <div class="glass-card overflow-hidden">
                <!-- Table Header -->
                <div class="p-6 border-b border-blue-100 bg-blue-50/50">
                    <div class="grid grid-cols-12 gap-3 text-blue-700 font-bold">
                        <div class="col-span-3">عنوان رویداد</div>
                        <div class="col-span-2">تاریخ رویداد</div>
                        <div class="col-span-2">وضعیت</div>
                        <div class="col-span-3">عملیات</div>
                    </div>
                </div>
                
                <div id="requests-container" class="max-h-[600px] overflow-y-auto">
                    <% if (requests && requests.length > 0) { %>
                        <% for (let request of requests) { 
                            // فرمت کردن تاریخ به صورت روز/ماه/سال
                            let formattedDate = '';
                            if (request.date) {
                                const date = new Date(request.date);
                                if (!isNaN(date)) {
                                    formattedDate = date.toLocaleDateString('fa-IR');
                                } else {
                                    // اگر فرمت تاریخ اشتباه بود، تلاش برای پارس کردن
                                    const parts = request.date.split('-');
                                    if (parts.length === 3) {
                                        formattedDate = `${parts[2]}/${parts[1]}/${parts[0]}`;
                                    } else {
                                        formattedDate = request.date;
                                    }
                                }
                            }
                            
                            // ترجمه وضعیت
                            let statusText = '';
                            let statusIcon = '';
                            switch(request.status) {
                                case 'pending':
                                    statusText = 'در انتظار';
                                    statusIcon = 'fa-clock';
                                    break;
                                case 'accepted':
                                    statusText = 'تایید شده';
                                    statusIcon = 'fa-check-circle';
                                    break;
                                case 'rejected':
                                    statusText = 'رد شده';
                                    statusIcon = 'fa-times-circle';
                                    break;
                                default:
                                    statusText = request.status;
                                    statusIcon = 'fa-question-circle';
                            }
                        %>
                            <div class="p-6 request-row request-item" data-id="<%= request.id %>">
                                <div class="grid grid-cols-12 gap-4 items-center">
                                    
                                    <div class="col-span-3">
                                        <div class="font-medium text-blue-900 mb-1">
                                            <%= request.title || 'بدون عنوان' %>
                                        </div>
                                        <% if (request.type) { %>
                                            <div class="text-sm text-blue-500">
                                                <%= request.type %>
                                            </div>
                                        <% } %>
                                    </div>
                                    
                                    <div class="col-span-2">
                                        <div class="font-medium text-blue-900">
                                            <%= formattedDate %>
                                        </div>
                                        <% if (request.time) { %>
                                            <div class="text-sm text-blue-500">
                                                <%= request.time %>
                                            </div>
                                        <% } %>
                                    </div>
                                    
                                    <div class="col-span-2">
                                        <span class="status-badge status-<%= request.status %>">
                                            <i class="fas <%= statusIcon %>"></i>
                                            <%= statusText %>
                                        </span>
                                    </div>
                                    
                                    <div class="col-span-2">
                                        <div class="flex gap-2">
                                            
                                            <% if (request.status === 'pending') { %>
                                                <button onclick="window.location.href='/venue-owner/accept-request/<%= request.id%>'" class="btn-approve action-btn" title="تایید سریع">
                                                    <i class="fas fa-check ml-1"></i>
                                                </button>
                                                <button onclick="window.location.href='/venue-owner/reject-request/<%= request.id%>'" class="btn-reject action-btn" title="رد سریع">
                                                    <i class="fas fa-times ml-1"></i>
                                                </button>
                                            <% } %>
                                            <% if (request.status === 'accepted') { %>
                                                <button onclick="viewContract('<%= request.id %>')" class="btn-secondary action-btn text-sm" title="مشاهده قرارداد">
                                                    <i class="fas fa-file-contract ml-1"></i>
                                                </button>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                    <% } else { %>
                        <!-- Empty State -->
                        <div id="emptyState" class="p-12 text-center">
                            <i class="fas fa-inbox text-6xl text-blue-300 mb-4"></i>
                            <h3 class="text-xl font-bold text-blue-900 mb-2">هیچ درخواستی یافت نشد</h3>
                            <p class="text-blue-600">برای این مکان هنوز درخواستی ثبت نشده است.</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </main>

        <!-- Full Form Modal -->
        <div id="fullFormModal" class="modal">
            <div class="glass-card max-w-5xl w-full mx-4 max-h-[90vh] overflow-y-auto">
                <div class="p-8">
                    <!-- Modal Header -->
                    <div class="flex justify-between items-start mb-6 sticky top-0 bg-white z-10 pb-6 border-b border-blue-100">
                        <div>
                            <h2 class="text-2xl font-bold text-blue-900 mb-2" id="modalEventTitle">فرم درخواست رویداد</h2>
                            <div class="flex flex-wrap items-center gap-3">
                                <span class="status-badge" id="modalStatus">در انتظار</span>
                                <span class="text-blue-600" id="modalTrackingCode">کد پیگیری: </span>
                                <span class="text-blue-600" id="modalSubmitDate"></span>
                                <span class="text-blue-600" id="modalvenueName"></span>
                            </div>
                        </div>
                        <button onclick="closeFullForm()" class="text-blue-400 hover:text-blue-600 p-2 text-xl">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                    
                    <!-- Modal Body -->
                    <div class="space-y-8" id="modalBody">
                        <!-- اطلاعات به صورت داینامیک پر می‌شود -->
                    </div>
                </div>
            </div>
        </div>

        <!-- Add venue Modal -->
        <div id="addvenueModal" class="modal">
            <div class="glass-card max-w-lg w-full mx-4">
                <div class="p-8">
                    <div class="flex justify-between items-start mb-6">
                        <div>
                            <h3 class="text-2xl font-bold text-blue-900 mb-2">افزودن مکان جدید</h3>
                            <p class="text-blue-600">اطلاعات مکان جدید را وارد کنید</p>
                        </div>
                        <button onclick="closeAddvenueModal()" class="text-blue-400 hover:text-blue-600 p-2">
                            <i class="fas fa-times text-xl"></i>
                        </button>
                    </div>
                    
                    <form id="addvenueForm" onsubmit="saveNewvenue(event)">
                        <div class="space-y-6">
                            <div>
                                <label class="block text-blue-700 mb-2">نام مکان <span class="text-red-500">*</span></label>
                                <input type="text" 
                                       class="w-full p-3 rounded-xl border border-blue-200 focus:border-blue-400 outline-none"
                                       placeholder="مثال: مرکز همایش‌های بین‌المللی"
                                       id="newvenueName" required>
                            </div>
                            
                            <div>
                                <label class="block text-blue-700 mb-2">شهر <span class="text-red-500">*</span></label>
                                <input type="text" 
                                       class="w-full p-3 rounded-xl border border-blue-200 focus:border-blue-400 outline-none"
                                       placeholder="مثال: تهران"
                                       id="newvenueCity" required>
                            </div>
                            
                            <div>
                                <label class="block text-blue-700 mb-2">آدرس کامل <span class="text-red-500">*</span></label>
                                <textarea class="w-full p-3 rounded-xl border border-blue-200 focus:border-blue-400 outline-none h-32"
                                          placeholder="آدرس کامل مکان..."
                                          id="newvenueAddress" required></textarea>
                            </div>
                            
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-blue-700 mb-2">ظرفیت (نفر)</label>
                                    <input type="number" 
                                           class="w-full p-3 rounded-xl border border-blue-200 focus:border-blue-400 outline-none"
                                           placeholder="۵۰۰"
                                           id="newvenueCapacity" min="1">
                                </div>
                                
                                <div>
                                    <label class="block text-blue-700 mb-2">قیمت پایه (تومان)</label>
                                    <input type="text" 
                                           class="w-full p-3 rounded-xl border border-blue-200 focus:border-blue-400 outline-none"
                                           placeholder="۱۰,۰۰۰,۰۰۰"
                                           id="newvenueBasePrice">
                                </div>
                            </div>
                            
                            <div>
                                <label class="block text-blue-700 mb-2">توضیحات</label>
                                <textarea class="w-full p-3 rounded-xl border border-blue-200 focus:border-blue-400 outline-none h-24"
                                          placeholder="توضیحات درباره مکان، امکانات، تجهیزات..."
                                          id="newvenueDescription"></textarea>
                            </div>
                            
                            <div class="flex gap-4 pt-6 border-t border-blue-200">
                                <button type="submit" class="flex-1 btn-primary">
                                    <i class="fas fa-save ml-2"></i> ذخیره مکان
                                </button>
                                <button type="button" onclick="closeAddvenueModal()" class="flex-1 btn-secondary">
                                    <i class="fas fa-times ml-2"></i> انصراف
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        let currentRequestId = null;

    

        // جستجوی درخواست‌ها
        function searchRequests() {
            const searchTerm = document.getElementById('searchInput').value.toLowerCase();
            const requestItems = document.querySelectorAll('.request-item');
            
            requestItems.forEach(item => {
                const text = item.textContent.toLowerCase();
                if (text.includes(searchTerm)) {
                    item.style.display = 'block';
                } else {
                    item.style.display = 'none';
                }
            });
        }

        // مشاهده فرم کامل درخواست
        function viewFullForm(requestId) {
            currentRequestId = requestId;
            
            // در حالت واقعی، اینجا باید درخواست AJAX برای دریافت اطلاعات کامل فرم ارسال شود
            // برای مثال، فرض می‌کنیم اطلاعات فرم در یک متغیر JavaScript وجود دارد
            
            // نمایش مودال
            document.getElementById('fullFormModal').classList.add('active');
            document.body.style.overflow = 'hidden';
            
            // پر کردن اطلاعات مودال با داده‌های نمونه
            // در حالت واقعی باید داده‌ها از سرور دریافت شوند
            loadFullFormData(requestId);
        }

        // بارگذاری اطلاعات فرم کامل
        function loadFullFormData(requestId) {
            // این یک نمونه است. در حالت واقعی باید از سرور داده دریافت کنید
            const sampleData = {
                eventTitle: "همایش نوآوری در فناوری",
                trackingCode: "EVT-2024-001",
                submitDate: "۱۴۰۳/۰۲/۱۰",
                venueName: "مرکز همایش‌های تهران",
                description: "درخواست برگزاری کنفرانس سالیانه فناوری با حضور ۳۰۰ نفر از متخصصان صنعت.",
                status: "pending"
            };
            
            document.getElementById('modalEventTitle').textContent = `فرم درخواست: ${sampleData.eventTitle}`;
            document.getElementById('modalTrackingCode').textContent = `کد پیگیری: ${sampleData.trackingCode}`;
            document.getElementById('modalSubmitDate').textContent = `ثبت: ${sampleData.submitDate}`;
            document.getElementById('modalvenueName').textContent = `مکان: ${sampleData.venueName}`;
            
            const statusBadge = document.getElementById('modalStatus');
            statusBadge.className = `status-badge status-${sampleData.status}`;
            statusBadge.innerHTML = sampleData.status === 'pending' ? 
                '<i class="fas fa-clock"></i> در انتظار' :
                '<i class="fas fa-check-circle"></i> تایید شده';
        }

        // بستن فرم کامل
        function closeFullForm() {
            document.getElementById('fullFormModal').classList.remove('active');
            document.body.style.overflow = 'auto';
            currentRequestId = null;
        }

        // // تایید سریع درخواست
         function quickApprove(requestId) {
            requestId = parseInt(requestId)
            if (confirm('آیا از تایید این درخواست اطمینان دارید؟')) {
                // در حالت واقعی درخواست به API ارسال می‌شود
                showNotification(`درخواست ${requestId} با موفقیت تایید شد`, 'success');
                // رفرش صفحه
                setTimeout(() => {
                    venue.reload();
                }, 1000);
            }
        }

        // رد سریع درخواست
        function quickReject(requestId) {
            requestId = parseInt(requestId)
            const reason = prompt('لطفاً دلیل رد درخواست را وارد کنید:');
            if (reason) {
                // در حالت واقعی درخواست به API ارسال می‌شود
                showNotification(`درخواست ${requestId} به دلیل "${reason}" رد شد`, 'error');
                // رفرش صفحه
                setTimeout(() => {
                    venue.reload();
                }, 1000);
            }
        }

        // مشاهده قرارداد
        function viewContract(requestId) {
            showNotification(`قرارداد درخواست ${requestId} در حال بارگذاری...`, 'info');
            // در حالت واقعی، کاربر به صفحه قرارداد هدایت می‌شود
        }

      
        // بروزرسانی داده‌ها
        function refreshData() {
            showNotification('در حال بروزرسانی داده‌ها...', 'info');
            venue.reload();
        }

        // نمایش مودال افزودن مکان جدید
        function showAddvenueModal() {
            document.getElementById('addvenueModal').classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        // بستن مودال افزودن مکان
        function closeAddvenueModal() {
            document.getElementById('addvenueModal').classList.remove('active');
            document.body.style.overflow = 'auto';
            document.getElementById('addvenueForm').reset();
        }

        // ذخیره مکان جدید
        function saveNewvenue(event) {
            event.preventDefault();
            
            const name = document.getElementById('newvenueName').value;
            const city = document.getElementById('newvenueCity').value;
            const address = document.getElementById('newvenueAddress').value;
            
            if (!name || !city || !address) {
                showNotification('لطفاً فیلدهای الزامی را پر کنید', 'error');
                return;
            }
            
            // در حالت واقعی، اینجا باید درخواست POST به سرور ارسال شود
            showNotification('مکان جدید با موفقیت افزوده شد', 'success');
            closeAddvenueModal();
            
        }